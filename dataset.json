[
  {
    "task": "Parse an AWS S3 bucket name from an S3 URI (e.g., s3://my-bucket/path/to/file.txt) and extract just the bucket name",
    "format": "regex"
  },
  {
    "task": "Write a Python function that takes an AWS IAM policy JSON string and returns True if it contains a wildcard (*) in the Resource field",
    "format": "python"
  },
  {
    "task": "Create a JSON CloudFormation template snippet that defines an AWS Lambda execution role with basic permissions",
    "format": "json"
  }
]